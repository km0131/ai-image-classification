services:
  python-api:
    build: .
    ports:
      - "5000:5000"  # ホストの5000番をコンテナの5000番に繋ぐ
    environment:
      - PORT=5000
      - PYTHON_API_SECRET=${PYTHON_API_SECRET}
      - CALLBACK_SECRET=${CALLBACK_SECRET}
    volumes:
      - ./received_images:/app/storage # 今のディレクトリにあるフォルダをマウント