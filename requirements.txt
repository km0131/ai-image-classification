# --- Web API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9

# --- 汎用 ---
python-dotenv>=1.0
numpy>=1.26,<2.0
requests>=2.32
opencv-python-headless>=4.10

# --- PyTorch/timm(3モデル共通基盤: mobilenet_v3 / efficientnet_lite4 / mobilevit_v2 / ResNet50特徴抽出) ---
# torch==2.6.0 は pip install 自体は通るが、litert-torch が依存する torchao が
# torch.utils._pytree.register_constant (torch 2.6系には存在しない) を要求するため
# import時にクラッシュする。litert-torch(pip制約 torch<2.13.0,>=2.4.0)と実際に組み合わせて
# 動作確認済みの2.10.0を使う。
torch==2.10.0
torchvision>=0.20
timm>=1.0

# --- TFLite変換/ランタイム ---
litert-torch
ai-edge-litert
